#!/usr/bin/env node
/**
 * ONE-TIME assembler used 2026-06-25 to merge plan files into BUILD_PLAN.md.
 *
 * Do NOT re-run unless you restore full source files from git history.
 * Redirect stub files (CLERK_SETUP.md, etc.) no longer contain embeddable content.
 */
console.error(
  'assemble-build-plan.mjs is disabled. Plan content lives in BUILD_PLAN.md only.',
);
process.exit(1);
